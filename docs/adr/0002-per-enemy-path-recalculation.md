# 每隻 Enemy 各自在抵達 Cell 中心時重新計算 Path

Grid 上的 Path 會隨玩家放置/賣出 Tower 而改變。若每次改變都讓所有 Enemy 立即重算並轉向,會在 Enemy 移動到一半時出現瞬間轉向的跳動感。因此改為:每隻 Enemy 只在自己抵達下一個 Cell 中心點時,才以「自己當前所在 Cell」為起點重新計算到 Goal 的最短 Path。

這個設計的代價是:同一時刻場上不同 Enemy 手上的 Path 可能不一致(有的還在走舊路徑,有的已經套用新路徑),這是刻意的取捨,不是 bug——換來的是移動視覺上的平滑度。
