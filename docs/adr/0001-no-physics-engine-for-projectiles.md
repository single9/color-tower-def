# 不使用 bevy_rapier2d,Projectile 碰撞改自寫距離判定

Projectile 命中 Enemy 只需要「鎖定目標並跟隨移動、進入某個距離閾值即視為命中」這種單純邏輯,不需要完整物理引擎的剛體、重力、多物體碰撞解算。引入 bevy_rapier2d 會增加依賴與學習成本,換來的功能在此專案用不到,因此改為每幀計算 Projectile 與目標 Enemy 的距離,小於閾值即判定命中。

若之後需要更複雜的效果(擊退、彈道偏移、Projectile 之間的物理互動),屆時需要重新評估是否引入正式物理引擎。
